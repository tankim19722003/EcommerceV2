const Icon = ({ children}) => (
  <a className="w-8 h-8 p-1 rounded-full cursor-pointer transition duration-300 ease-in-out flex items-center justify-center">
    {children}
  </a>
);

export default Icon;